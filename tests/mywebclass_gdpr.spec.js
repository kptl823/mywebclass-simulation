import { test, expect } from '@playwright/test';
test('Clicking on Google Analytics Policy link navigates to the correct link', async ({ page }) => {  // Navigate to the webpage  await page.goto('https://github.com/kptl823/mywebclass-simulation/blob/master/src/index.html');
  // Click on the "Google Analytics Policy" link  const page1Promise = page.waitForEvent('popup');  await page.getByRole('link', { name: 'Google Analytics policy' }).click();  const page1 = await page1Promise;
  // Verify that the page navigated to the correct link  const url = page1.url();  expect(url).toBe('https://marketingplatform.google.com/about/analytics/terms/us/');});